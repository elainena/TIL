//3.1 Interpolation 
{{ example }}

import { Component } from '@angular/core';

@Component({
    selector: 'app-root',
    template:
})

export class AppComponent {
    name = 'Angular';
    age = 20;
    admin = true;
    address = {
        city : 'Seoul',
        country : 'Korea'
    };
}
